# Backlog 0003: Coffer web client — prototype core loop on mock data

**Status:** Open  ·  **Priority:** High  ·  **Surfaces:** app  ·  **Opened:** 2026-08-22
**Epic:** Foundation — first usable expense tracker

## Context / problem

Every screen in this product is still hypothetical. The Epic's phase 2 note says the API response shape "is a contract — pin it in the ticket before phase 3 starts", and today there is nothing to pin it against: we would be designing a JSON shape with no idea what the ledger view or the entry form actually needs from it.

A clickable prototype on mock data inverts that. The client is built first against a mock network layer, and the mock handlers become the **draft** of the phase-2 API contract — written by the consumer that has to live with it, rather than guessed at by the producer.

The prototype also carries auth screens. That does **not** mean the product has auth: the Owner confirmed on 2026-08-22 that this stays localhost-only, so the CANDIDATES question "Does this ever leave localhost?" remains open and no ADR is made here. Login is prototyped as a UI surface so the shell has a real unauthenticated/authenticated split; the day hosting is wanted, auth + TLS + an `ops` gate get ticketed together and this prototype is what they render.

**Auto-persisted:** No — Owner approved 2026-08-22. Fails the rubric on three signals: change type (addition), product decision (auth shape, product name), contract (the mock handlers draft the API response shape).

## Goal & non-goals

- **Goal:** a runnable web client where the Owner can log in, see a ledger, enter a transaction, watch it appear with a correct amount, correct a mistake, and triage what is uncategorized — all against mocked network responses, no backend running. **This is the core loop: the smallest thing that is genuinely usable and can be judged.**
- **Scope was chosen by the Owner on 2026-08-22** from a candidate function list of sixteen; thirteen were selected. This ticket carries five of them and [0004](0004-app-prototype-accounts-transfers-insight.md) carries the other eight. The three declined are named as non-goals below so they are not quietly reintroduced.
- **Goal:** MSW request handlers that are readable as a proposed API contract, so phase 2 of the Epic can be ticketed from evidence instead of speculation.
- **Non-goal: real authentication.** No password hashing, no server-side session store, no credential leaving the browser, and no password reset. The screens are real; what sits behind them is not. ~~*and no second account — first-run setup provisions exactly one*~~ — **struck 2026-08-25** by the third Owner directive below and [ADR 0004](../decisions/0004-multi-user-tenant-scoped-from-day-one.md): sign up is open and any number of accounts may exist. Left visible rather than deleted, because an agent reading top-down hits this line before the directive.
- **Non-goal:** any work in `../projects/api`. This ticket does not touch the backend surface.
- **Non-goal:** locking the API contract. The handlers are a *draft*; promoting them to the pinned contract is a separate Owner-gated step.
- **Non-goal:** import, budgets, recurring transactions, and nested categories — all four are unresolved in CANDIDATES. Categories here are **flat**.
- **Non-goal — declined by the Owner, 2026-08-22:** spending-over-time trend (needs months of real data to say anything), CSV/JSON export (would export fixtures and prove little now), and a settings screen (currency and date format are fixed by ADR 0003, leaving little to put in it). Each is a reasonable later ticket, not a gap in this one.
- **Non-goal — deferred to [0004](0004-app-prototype-accounts-transfers-insight.md), not dropped:** accounts management, transfers between accounts, category CRUD, the month summary, spending by category, and the dashboard. **There is no dashboard yet** — until 0004 lands, a successful login lands on **the ledger**; 0004 introduces the dashboard and moves the landing route there.
- **Non-goal:** visual polish as a deliverable. This is a prototype for shaping the contract and the entry flow, not a design sign-off.
- **Non-goal: automated tests.** Owner decision, 2026-08-22. Closing evidence is a green build plus observed dev-server behavior, not test counts.

## Dependencies

- **0002 must land first.** The money formatting/parsing module is the client's half of the money contract; screens built before it exists will duplicate amount formatting inline, which is the exact failure 0002 was opened to prevent. Every amount rendered in this ticket goes through that module — no local formatting.
- **Product name is resolved:** the product is **Coffer** ([ADR 0002](../decisions/0002-product-name-coffer.md)). The shell, the browser title, and the login screen carry that name — no placeholder.

## Plan (by phase)

Five Owner-selected functions, grouped so each phase is independently demonstrable.

**Owner directive — 2026-08-25 (third): conventional login + sign up.** *"tôi muốn có màn hình login & sign up dạng truyền thống, để có thể sau này đẩy lên cloud cho mọi người dùng"*

This replaces phase 2's first-run-provisioning model. **Both screens are permanently reachable**, cross-linked the way any signed-up product does it — sign up is no longer a screen that disappears once one account exists.

**It also puts a written product constraint under revision, which is why it is recorded here and not just built.** `CLAUDE.md` opens with *"Single user, no multi-tenancy. There is no organization, no sharing, no roles. Don't build for tenants that don't exist."* "Đẩy lên cloud cho mọi người dùng" is exactly that tenant. The constraint is not overridden by this entry — only the Owner can do that, and it is a `CLAUDE.md` edit plus an ADR, not a ticket note.

**Split, because the two halves cost different amounts:**
- **The screens are cheap and are being built now** (phase 2b below). A prototype's sign-up screen is a form; it commits nothing.
- **Multi-tenancy is not cheap and is not being built.** It reaches every table, every query, and every endpoint. **The question is in front of the Owner now rather than later because [0001](0001-api-ledger-foundation.md) has not written a line of schema yet** — a `user_id` added to an empty schema is free; retrofitted onto real rows it is a migration with a data-loss edge. See [CANDIDATES](../decisions/CANDIDATES.md).

**Note for the surface agent:** the conflict between this directive and `CLAUDE.md`'s single-user line is **known and already escalated**. Build the screens; do not halt on it.

**Owner directive — 2026-08-25 (second, and it reverses this ticket's premise).** After phase 2 landed, the Owner read the shape of what had been built and stopped it: *"khi nói về mock data, dường như bạn đã làm quá lên, thực sự mong muốn của tôi chỉ là build ui và bấm qua lại đc, để xây dựng ui ux, chúng ta đâu cần mock server?"*

The measurement that settled it: **1.343 lines of mock-server machinery against 858 lines of actual UI**, of which ~400 lines were a fake auth server (`POST /api/session`, bearer tokens, 401/409) standing behind a login screen whose real job is to let the Owner click through to the next screen.

**What is now true:**

- **The mock network layer is deleted, not paused.** MSW, the service worker, `handlers.ts`, `store.ts`, `owner-store.ts`, `api/client.ts`, `dev-server/` — all out. Screens import JSON directly and hold their state in React. Login flips a boolean; it does not call an endpoint.
- **This ticket no longer drafts the API contract.** That was its stated reason for existing ("the mock handlers become the *draft* of the phase-2 API contract, written by the consumer"). The Owner has chosen click-through UI/UX over contract drafting. **The Epic's phase 2 must now define its own request/response shapes** — nothing downstream produces a draft for it any more. This is recorded in `STATUS.md`'s Epic table so `api` cannot inherit a draft that stopped existing.
- **What survives the deletion:** the JSON fixtures, the money module, the calendar-date module, the type definitions (they are where the money contract lives in the client), and every screen built so far — ledger, filters, setup, login, shell.
- **Transaction entry keeps a visible loop.** State is lifted to one shared place rather than scattered per-route, so a transaction entered in phase 4 appears in the ledger during the session. Entry speed is the product's stated feature and cannot be judged if a new row vanishes on submit. A reload re-seeds from JSON — accepted by the Owner.
- **PM note, for the record:** the over-build was the PM's brief, not the agent's improvisation. The agent built what it was told and flagged its own additions. The 2026-08-22 framing invited contract-drafting, but nothing forced the PM to commission a fake auth server for a login screen — the conflict should have gone to the Owner before delegation, not after.

**Owner directive — 2026-08-25 (first).** Two instructions, given together as "xây dựng ui từ màn hình đăng nhập, dữ liệu giả để thiết kế dưới dạng json":

- **Phase 2 is next**, ahead of phase 4. The PM had recommended phase 4 (quick entry) as the higher-value next step; the Owner chose to build the UI starting from the login screen. Phase 4 keeps its priority and follows.
- **Seed data moves out of TypeScript into JSON.** `src/mocks/fixtures.ts` becomes JSON files the Owner can hand-edit to reshape a screen without touching code. Two constraints ride along, because JSON drops what the current file carries:
  - **The rationale must survive.** `fixtures.ts`'s header documents why each awkward case exists — the zero-amount row, the ~10^9 row, the account with no transactions, the uncategorized rows. JSON has no comments, so that text moves to a `README.md` beside the JSON. A future agent that cannot see why those rows exist will trim them as noise, and the prototype quietly stops testing the cases it was built to test.
  - **The typed boundary stays.** The JSON is imported and checked against the `api/types.ts` types at the import site, so a malformed fixture fails `tsc` rather than surfacing as an empty screen at runtime.

1. **Phase 1 — `app`:** wire MSW into the Vite dev server as a browser worker, with handlers backed by a small in-memory fixture store (accounts, categories, transactions) so writes are visible on subsequent reads. Fixtures reflect how money actually moves here — a cash account, a bank account, and an e-wallet — and cover the awkward cases, not just the happy path: negative and positive amounts, zero, a large amount (~10⁹ đồng), an uncategorized transaction, and an account with no transactions.
2. **Phase 2 — `app`:** **auth surface — first-run setup, then login.** Owner decision, 2026-08-22, replacing this ticket's earlier "no registration screen" position. The two are reconciled by *when* the screen appears rather than whether it exists:
   - **First-run setup** — shown only while **no account exists**. Email + password + confirm, creating the product's one account. This is the real problem self-hosting has: provisioning the single credential. It is not a signup form in the multi-tenant sense, and there is never a "second account".
   - **Login** — shown once an account exists. The setup route redirects to login from then on, and login redirects to setup while no account exists. Neither is reachable in the wrong state.
   - **Authenticated shell, route protection, and logout.** Every app route sits behind the session; logout returns to login, not to setup.
   - Mock `POST /session` for login; wrong credentials return a realistic failure the form actually renders (not a silent no-op). Mock account creation for setup.
   - **Prototype-only, and this is load-bearing:** no password hashing, no real session store, no credential ever leaving the browser. The real thing arrives with the hosting decision (see CANDIDATES) and brings TLS and an `ops` gate with it.
   - **Persist the created account and the session** (browser storage is fine) so a page reload does not wipe the account and drop you back into first-run. Without this, the login screen is unreachable in practice and half of what this phase builds cannot be looked at. The fixture store otherwise starts with **no account**, so the first-run path is what the Owner sees on a fresh load.
3. **Phase 3 — `app`:** **the ledger**, as the landing route for now — transactions in date order, amounts via the 0002 module, sign rendered as direction. Plus **filter & search**: date range, account, category, and description text. Filters belong in the request, not in client-side array filtering — they are part of the contract this prototype is drafting, and client-side filtering drafts nothing.
4. **Phase 4 — `app`:** **quick entry** and **edit / delete**. **Entry speed is the stated product feature, so this is the phase that matters most** — keyboard-first, no modal round-trips, amount parsing through the 0002 module with malformed input rejected with a reason rather than coerced. The category field is skippable, which is what makes phase 5 necessary.
5. **Phase 5 — `app`:** **uncategorized triage inbox** — everything with no category, cleared in a batch. This is the other half of fast entry: skip the category while typing, sort it out later.
6. **Phase 6 — `app`:** a dev-server walkthrough of the core loop — log in, enter a transaction without a category, see it in the ledger correctly formatted, correct its amount, find it by filter, then categorize it from the triage inbox — reported as observed behavior.

## Contract

The prototype honors the money contract from CLAUDE.md and the field names pinned in 0001, so the mock and the eventual API do not diverge:

- Amounts are signed integers in minor units, JSON field `amount_minor` as a **number**. No floats, no decimal strings, no client-side arithmetic that leaves integer space.
- **Currency is VND, exponent 0** ([ADR 0003](../decisions/0003-currency-vnd-single-exponent-zero.md)). Fixtures are stated in đồng — a 30.000 ₫ coffee is `amount_minor: 30000`, **not** `3000000`. Every rendered amount goes through the 0002 module; no component formats or divides on its own.
- Sign is direction: outflow negative, inflow positive. Neither category nor account may imply it.
- `occurred_on` is a calendar date `YYYY-MM-DD`, never a timestamp.
- No per-row currency field, no currency column, no conversion.
- No `balance` field on an account — it is derived.

**What this ticket produces is a draft, not the pinned contract.** When the prototype lands, the PM extracts the handler shapes into the Epic's phase-2 ticket for the Owner's go. `api` does not build against these handlers until that happens.

## Outcome

<!-- Filled per phase by the PM from the sub-agent's evidence. -->

- **Phase 1 — DONE 2026-08-22.** MSW browser worker + in-memory fixture store. Files: `src/mocks/{fixtures,store,handlers,browser,start}.ts`, `src/api/{client,types}.ts`, `dev-server/mockServiceWorker.js`, `vite.config.ts` (`apply:'serve'` plugin), `package.json` (`msw@2.15.0`, devDependency). 56 transactions · 4 accounts · 8 categories, realistic VND magnitudes.
  **PM-verified independently:** `npm run build` → `tsc -b && vite build` green, `✓ built in 132ms`. **MSW absent from the production bundle** — `grep -ric msw|mockServiceWorker|setupWorker dist/assets/*.js` → **0 hits**; fixture strings (`acc_momo`, `Vietcombank`) → **0 hits**; `dist/` is exactly 3 files. Writes visible on reads confirmed by the agent (POST → 201 `txn_057`, refetch shows it); `POST` with `amount_minor: -40.5` → `400 invalid_amount`.
- **Phase 2 — DONE 2026-08-25.** Auth surface + the fixtures-to-JSON directive, built together.
  **Files** — *JSON move:* `src/mocks/fixtures/{accounts,categories,transactions}.json` (4 / 8 / 56 rows, one row per line, hand-editable) + `src/mocks/fixtures/README.md`; `src/mocks/fixtures.ts` reduced to the typed boundary. *Auth:* `src/auth/{AuthContext,AuthProvider,AuthGate,AuthScreen,AuthError,auth-error-code,session-token,useAuth}`, `src/routes/setup/SetupView.tsx`, `src/routes/login/LoginView.tsx`, `src/mocks/owner-store.ts`; modified `src/api/{types,client}.ts`, `src/mocks/handlers.ts`, `src/App.tsx`, `src/AppShell.tsx`, `src/main.tsx`, `src/copy/strings.ts`, `src/mocks/start.ts`, `src/routes/ledger/LedgerView.tsx`.
  **PM-verified independently** — `npm run build` green (`✓ 44 modules transformed`, `✓ built in 91ms`). A PM-written CDP scenario (own assertions, fresh Chrome profile, not the agent's script): **10/10 pass** —
  fresh load with empty `localStorage` → **setup**, not login (`storage=[]`); `/login` with no account → setup; setup completed → `/` **ledger, 56 rows**, `data-owner-email=pm@coffer.local`; **reload → still authenticated**, 56 rows; logout → **`/login`** with `coffer.session.token` gone but `coffer.mock.owner` retained (account survives, session doesn't); protected route while logged out → `/login` with `#root` at 1250 chars, not blank; wrong password → `data-auth-error="invalid_credentials"`, `role="alert"`, rendered text `Email hoặc mật khẩu không đúng.`; `/setup` once an account exists → `/login`.
  **Money contract survived the JSON move** — observed `-1.250.000.000 ₫` (`outflow`) and `0 ₫` (`zero`, neutral). PM-checked the JSON directly: every `amount_minor` an integer, every `occurred_on` a `YYYY-MM-DD`, **no currency field on any row**, the 4 uncategorized rows intact.
  **Rationale survived** — `fixtures/README.md` names `txn_047`, `txn_033`, `txn_017`, `txn_052`, `txn_056`, `acc_tpb_savings` and the shared-date groups, so a later agent cannot mistake the awkward rows for noise. **Typed boundary holds** — `satisfies` at the import site, plus a runtime narrow for `Account["kind"]` (TypeScript widens a JSON string literal, so that one union cannot be checked statically; a bad kind now fails with a named error instead of an empty screen).
  **Mock layer still absent from production** — PM-grepped `dist/`: `msw`, `setupWorker`, `mockServiceWorker`, `acc_momo`, `Vietcombank`, `coffer.mock`, `txn_033` → **0 hits each**; `dist/` is 3 files. **No crypto dependency added** — deps are react, react-dom, react-router-dom; `msw` is still devDependencies only. Prototype-only auth held: no hashing, no server session store, no credential leaves the browser.

  **Flagged by the agent, PM-routed — not silently accepted:**
  1. **The data routes are not session-protected in the draft.** `GET /api/transactions` takes no token. Deliberate: a browser mock has nothing to protect, and adding a check would draft an auth scheme the hosting decision hasn't made. **`api` must not read the absence as a decision** — carried into the Epic's phase-2 row so the extraction states it rather than inherits it.
  2. **`Session` is `{ token }` and nothing else** — no expiry, refresh, or rotation. Under-drafted on purpose; must not read as "sessions never expire".
  3. **Four shapes invented with no decision behind them** — `GET /api/session` answers **200** for an anonymous caller (status endpoint: "not logged in" is a valid answer, and it resolves setup-vs-login-vs-app in one round trip); the route is `POST /api/setup` rather than `POST /api/owners` (no collection exists, and a plural route advertises a second account); minimum password length 8; a basic email shape. All four are Owner-gated **at contract promotion**, not now.
  4. **Three scope additions the agent made and declared.** A fifth auth state `unavailable` (a production build has no mock behind `/api`; without it `vite preview` is a white screen); `main.tsx` renders a failed bootstrap into `#root` rather than dying silently (this is what surfaces the `Account["kind"]` runtime narrow); and a dev-only `window.coffer.resetOwner()` — first-run is a screen you can otherwise see exactly once. PM accepts all three: each exists to make a state observable, which is the raised bar this ticket trades tests for.
  5. **`../projects/app/documents/architecture/01-overview.md` is now stale** — its "Shape" line still reads `routed views → feature components → typed API client → api service` with no mention of the gate that now fronts every route, and its "shared store only where genuinely cross-cutting" line has acquired its first real instance. Not fixed by the agent (correctly — it flagged rather than drifted). **Owed before phase 4 starts**, since that doc is the law the next agent reads first.
- **Phase 3 — DONE 2026-08-22.** Ledger with filters. Files: `src/routes/ledger/{LedgerView,TransactionTable,LedgerFilters,AmountCell}.tsx`, `useLedger.ts`, `src/lib/calendar-date.ts`, `src/copy/strings.ts`, `src/main.tsx`.
  **PM-verified independently via CDP** (headless Chrome, polled for `[data-status="ready"]`): unfiltered `/` → 56 rows, `reqUrl=/api/transactions`. **Filters proven to be in the request, not client-side:** `/?q=ca phe` → `?q=ca+phe`, **9 rows** (diacritic-insensitive — matched `Cà phê`); `/?category_id=none` → **4 rows**; `/?account_id=acc_tpb_savings` → **0 rows** + no-match empty state.
  **Money contract holds at every magnitude** — observed rendered strings: `-30.000 ₫`, `-45.000 ₫`, `25.000.000 ₫`, and the ~10⁹ case `-1.250.000.000 ₫` at full magnitude with no truncation or layout break. `0 ₫` renders **neutral**, not red — zero has no direction. Outflow red / inflow green, uncategorized as a `Chưa phân loại` chip.
- **Mock-layer strip — DONE 2026-08-25**, per the Owner's second directive.
  **Deleted (11 files):** `src/api/{client,types}.ts`, `src/mocks/*` (handlers, store, owner-store, fixtures, start, browser), `dev-server/mockServiceWorker.js`, `src/auth/{session-token,auth-error-code}.ts`. `msw` gone from `package.json`; `vite.config.ts` 36 → 7 lines.
  **Created:** `src/data/{accounts,categories,transactions}.json` + `README.md` (moved, rationale and the named ids intact), `src/data/types.ts` (transport types dropped, money-contract types kept), `src/data/seed.ts` (a *function*, so a bad hand-edit throws where `main.tsx` can catch it), `src/state/{AppDataContext,AppDataProvider,useAppData}` — 101 lines of `useState` + context, no store or repository.
  **Ratio the Owner objected to is closed:** mock/network machinery `1.343 → 0` lines; the surface is `2.962 → 1.618`, **−1.344**, and what remains is screens, shared state, the money/date modules, types, and copy.
  **PM-verified independently** — build green (`✓ 48 modules transformed`, `✓ built in 88ms`). Static: `grep -ri msw src/ vite.config.ts package.json` → **0**; `grep -rn 'fetch(' src/` → **0**; no `src/mocks/`, no `dev-server/`; dependencies are react, react-dom, react-router-dom and nothing else.
  A **PM-written CDP scenario on a PM-written driver** (the agent had overwritten the shared one — own driver, own assertions, fresh Chrome profile): **10/10 pass** — fresh load → setup with `localStorage` empty; setup → ledger **56 rows**; **0 service workers registered**; the five real filter controls present (`from`, `to`, `account_id`, `category_id`, `q`); `q="ca phe"` → **9 rows and all 9 contain `Cà phê`** (the diacritic fold survived the move client-side); reset → 56; logout → `/login`; wrong password → `Email hoặc mật khẩu không đúng.` still rendered; reload → **re-seeds to setup**, which is the accepted trade, not a regression.
  **Money contract intact after the strip** — `-1.250.000.000 ₫` outflow, `0 ₫` zero/neutral, and **zero** amounts rendered with a plain space instead of U+00A0 before `₫`.
  **Two notes carried forward, neither a defect:** `addTransaction` exists in `src/state/` and nothing calls it yet — phase 4 wires a form to it and stops. And fixture strings are now legitimately **in** the production bundle; phase 1/2's "fixtures absent from `dist/`" evidence is void and must not be re-asserted, because the JSON *is* the app's data now.
- **Phase 2b — DONE 2026-08-25.** Conventional login + sign up.
  **Files:** `src/routes/signup/SignupView.tsx` (new, replaces the deleted `routes/setup/`), `src/routes/login/LoginView.tsx`, `src/App.tsx`, `src/auth/{AuthGate,AuthContext,AuthProvider,AuthScreen}`, `src/AppShell.tsx`, `src/copy/strings.ts`, `src/data/types.ts` (`Owner` → `User`), `src/data/README.md`.
  **PM-verified independently** — build green (`✓ 48 modules transformed`, `✓ built in 89ms`); a PM-written CDP scenario, **10/10 pass**: `/signup` and `/login` each reachable on a direct fresh load with **zero accounts** and **neither redirects to the other**; cross-links present both ways; sign up → ledger **56 rows**, `data-account-count=1`; signed in, `/login` and `/signup` both bounce to the ledger; **two accounts created in one session** (`data-account-count=2`), each signing in with **its own** password and `b@`'s email with `a@`'s password rejected — the single-owner assumption is gone from the state, not just the routing; duplicate email → `email_taken`, `Email này đã được đăng ký. Hãy đăng nhập.`; `localStorage` and `sessionStorage` both empty, no service-worker controller.
  **Account enumeration checked, and the agent's answer was better than the PM's question.** The brief asked it to report whether a wrong password and an unknown email were *distinguishable*, on the assumption they would be. They are not: both return `invalid_credentials` with the identical rendered string `Email hoặc mật khẩu không đúng.` — PM-confirmed by driving both paths and comparing the code and the text. The agent also removed the last enumeration channel the previous build had (login no longer redirects to sign up when no account matches). **What remains for `api` when this becomes real: timing-based enumeration survives a shared error string, and that is a server-side concern.**
  **Judgment calls the agent made, all accepted:** `/setup` redirects to `/signup` rather than 404 — it is a URL in the Owner's history and in this ticket's own evidence, and a dead end in a click-through prototype reads as a broken app; `Owner` → `User` throughout, since the word encoded the assumption ADR 0004 has now reversed, kept clear of `Account` which is a *money* account; and the sign-out affordance became a bordered button showing the signed-in email, with the accessible name `Đăng xuất khỏi <email>` — with more than one account per session, *which* account you are in became a question the header has to answer.
  **Design observation, agent's, worth the Owner's attention:** the login card reads slightly bare without a forgot-password link — an ordinary login has three affordances below the password field and this one has two. Still not built: it needs email delivery, which nobody has decided, and a link to nowhere teaches the wrong thing about how finished this is. The slot exists (`AuthScreen`'s `footer`) for when that decision lands.
  **No test account is seeded** — the account list starts empty, so a fresh load lands on sign up. `test@test.com` / `12345678` is what the PM used to verify; anything matching `x@y.z` with 8+ characters works, and it lives until reload.
- **Docs realigned to ADR 0004 — DONE 2026-08-25.** `app/documents/architecture/01-overview.md` + `coding-conventions.md`. PM-verified: the ADR relative link resolves; the auth bullet now names the real field types (`accounts: readonly User[]`, `currentUserId`, `status` *derived*) instead of "flips a boolean"; a new subsection records the multi-account model as **settled law, not a mocking artifact**, opening "Read this before touching anything under `src/auth/`" and stating that collapsing it back to one owner is a regression. The anti-inference guard the PM asked for is in and explicit: ADR 0004 is a *data-model* decision binding on `api` and **buys the client nothing at runtime** — still no network, no persistence, no hashing, and a reload still re-seeds to zero accounts, which is *not* a reason to reach for `localStorage`. `coding-conventions.md` gained the `*Last updated*` stamp it was missing, which is why its staleness went a cycle undetected.
- **Owed to the next `app` brief (small, do not open a ticket for it):** `src/auth/AuthProvider.tsx:25` still says the single-user conflict "is escalated to the Owner (hub `decisions/CANDIDATES.md`)". That escalation was **resolved** by ADR 0004 — the comment now invites a future agent to re-open a closed decision. Line 15 of the same file is *correct* and must stay: hosting and real auth genuinely are still open in CANDIDATES.
- Phase 4: `<files, evidence>`
- Phase 5: `<files, evidence>`
- Phase 6: `<files, evidence>`
- **Harness delta (strip):** three, and the first one **corrects a claim this workspace had been repeating.**
  1. **`--dump-dom` did not become usable again once the app mounted synchronously — and the reason the agent doc gave for that was wrong.** The doc blamed the async MSW bootstrap; deleting it changed nothing. `createRoot().render()` commits in a scheduler task *after* the load event, so the load-event snapshot beats React to the DOM on **any React 18+ root**, sync or not. In Chrome 151 `--headless=new` the process also never exits. **The PM predicted the opposite in the delegation brief** — a wrong rationale attached to a right rule is what invited that, which is the general lesson: a rule recorded without its true cause gets relaxed by the next person who thinks the cause is gone.
  2. **No `timeout`/`gtimeout` on this box** — a watchdog around a hanging browser needs a shell loop over `kill -0`, or a tool call burns its whole budget.
  3. **`Runtime.evaluate` has no top-level `await`** — wrap in `(async () => …)()` with `awaitPromise: true`, or a malformed check reads as a failing one.
  *(Folded into `.claude/agents/app.md` in the same cycle.)*
- **Harness delta (phase 2):** six, folded into `.claude/agents/app.md`.
  1. **`[data-status="ready"]` alone stopped being a sufficient poll** the moment there was more than one view: an *in-page* transition leaves the old view still matching, so the poll returns instantly against stale DOM. Poll the compound `[data-view="X"][data-status="ready"]`.
  2. **Every terminal state must carry `data-status="ready"` — error states included.** A poll that only accepts the happy state hangs forever on a screen the app is never leaving.
  3. **Vite binds `::1` only** — `http://127.0.0.1:<port>` is connection-refused; use `localhost`. Chrome's CDP port is the opposite (v4), so `127.0.0.1:<debug-port>` is right. Mixing them up reads as a dead server.
  4. **`Intl.NumberFormat('vi-VN')` emits U+00A0 before `₫`.** Asserting `'0 ₫'` with a plain space fails against *correct* output. Assert `'0\u00a0₫'`.
  5. **Chrome logs every non-2xx as a console error**, so "console clean" cannot be `length === 0` on a suite that drives 401/409 paths deliberately. Classify expected vs unexpected.
  6. **A fresh `--user-data-dir` per run is load-bearing now, not hygiene** — the app persists to `localStorage`, so "empty browser storage" stops being a free precondition once any run has touched the profile. React controlled inputs also need the native value setter plus a bubbling `input` event; assigning `.value` does nothing.
  **PM's own delta:** a CDP `waitFor` expression must return a **boolean** — `document.querySelector(...)` under `returnByValue: true` fails to serialize a DOM node and the poll times out against a page that is actually correct. Use `!!document.querySelector(...)`. Cost the PM one false failure on this very ticket.
- **Harness delta (phases 1+3):**
  1. **`--dump-dom` is not a render check for any app that awaits something before mounting.** It snapshots at the load event, before the MSW worker registers, and returns an empty `#root` — reading as a failure that isn't one. `--virtual-time-budget` does not fix it and hangs against a service worker. **This supersedes the recipe folded into `.claude/agents/app.md` at 0002's close, which was right for a synchronous shell and wrong the moment the app became async.** The reliable form is CDP: launch with `--remote-debugging-port`, poll for a readiness attribute, then read `outerHTML`. → **Folded into `.claude/agents/app.md`.**
  2. **Render assertion attributes onto the DOM.** `data-request-url`, `data-result-count`, `data-direction` turned "filters are in the request" and "sign is direction" from claims into greppable evidence, and are what let the PM re-verify in seconds rather than re-reading the source. Cheap; worth doing by default on any view whose correctness is about what it fetched. → **Folded into `.claude/agents/app.md`.**
